{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.14.0/.schema/devbox.schema.json",
  "packages": ["nodejs@latest"],
  "shell": {
    "scripts": {
      "init_hook": ["unset DEVELOPER_DIR", "npm install"],
      "test": ["echo \"Error: no test specified\" && exit 1"]
    }
  }
}
